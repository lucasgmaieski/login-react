import { styled } from 'styled-components';

export const Container = styled.div`
    width: 600px;
    margin: 0 auto;
    form {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }
`;